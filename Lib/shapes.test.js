const { Triangle, Circle, Square } = require("./shapes.js");

describe("Test Triangle",() => {
    test("test for triangle with a orange background", () => {
        const shape = new Triangle(); 
    }
    )
})


    


